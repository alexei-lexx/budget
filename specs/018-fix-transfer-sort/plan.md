# Implementation Plan: Transfer Transaction Sort Order Fix

**Branch**: `018-fix-transfer-sort` | **Date**: 2025-12-06 | **Spec**: [spec.md](./spec.md)
**Input**: Feature specification from `/specs/018-fix-transfer-sort/spec.md`

**Note**: This plan was generated by the `/speckit.plan` command.

## Summary

Fix the ordering of paired transfer transactions in the transactions list by implementing deterministic database-level sorting. Currently, transfer transactions created with identical timestamps appear in unpredictable order. This fix introduces a composite sort key (`createdAtId = createdAt + "#" + id`) combined with ULID-based IDs to ensure TRANSFER_IN transactions consistently appear before TRANSFER_OUT transactions when sorted newest-first.

**Technical Approach**:
- Replace UUID v4 IDs with ULIDs (lexicographically sortable by creation time)
- Add `createdAtId` composite field to DynamoDB items only (NOT in Transaction interface)
- Replace `UserCreatedAtIndex` (userId + createdAt) with `UserCreatedAtIdIndex` (userId + createdAtId)
- Populate existing records via data migration
- Keep `createdAtId` database-only (not in model interface, not exposed to service/GraphQL/frontend)

## Technical Context

**Language/Version**: TypeScript (Node.js runtime)
**Primary Dependencies**: Apollo Server, AWS SDK v3, DynamoDB, ulidx (new)
**Storage**: DynamoDB with Global Secondary Indexes
**Testing**: Jest
**Target Platform**: AWS Lambda (backend) + local development (DynamoDB Local)
**Project Type**: Web application (backend + frontend)
**Performance Goals**: Maintain current query performance (<200ms p95 for transaction list queries)
**Constraints**:
- No breaking API changes (GraphQL schema unchanged)
- Migration must be idempotent and safe for production
- Maintain backward compatibility during deployment window
**Scale/Scope**:
- Database-level change affecting Transactions table
- Repository layer modification only (service/resolver/frontend unchanged)
- Single migration to populate ~1000s of existing transaction records

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

### ✅ Repository Structure
- **Compliant**: Four independent packages (backend, frontend, backend-cdk, frontend-cdk)
- **Impact**: Backend package modified (repository + model), backend-cdk modified (infrastructure)

### ✅ Vendor Independence
- **Compliant**: Uses ULID library (portable across databases), composite key pattern is database-agnostic
- **Data Layer**: Repository pattern preserved, only internal implementation changes

### ✅ Schema-Driven Development
- **Compliant**: No GraphQL schema changes required
- **Impact**: Backend TypeScript types updated (Transaction interface)

### ✅ Backend Layer Structure
- **Compliant**: Changes isolated to repository layer
- **Service Layer**: No modifications (unaware of `createdAtId`)
- **GraphQL Layer**: No modifications

### ✅ GraphQL Pagination Strategy
- **Compliant**: Maintains existing Relay-compatible cursor pagination
- **Impact**: Cursor format changes internally but API contract unchanged

### ✅ Database Record Hydration
- **Compliant**: Transaction Zod schema unchanged - hydration strips `createdAtId` from raw items
- **Implementation**: `createdAtId` excluded from Transaction interface, automatically removed during hydration

### ✅ Soft-Deletion
- **Compliant**: No changes to soft-deletion logic

### ✅ Data Migrations
- **Compliant**: Migration follows framework from constitution
- **File**: `backend/src/migrations/[timestamp]-populate-createdAtId.ts`
- **Idempotency**: Checks for existing `createdAtId` before updating

### ✅ Authentication & Authorization
- **Compliant**: No changes to auth flows

### ✅ Test Strategy
- **Action Required**: Update repository tests for new field and ULID generation
- **Service Tests**: No changes (service layer unaffected)

### ✅ Input Validation
- **Compliant**: No new user input (internal field only)

### ✅ TypeScript Code Quality
- **Action Required**: Run `npm run format` and fix ESLint issues after changes

### Gate Status: ✅ PASS

No constitution violations. All changes align with established patterns.

## Project Structure

### Documentation (this feature)

```text
specs/018-fix-transfer-sort/
├── plan.md              # This file
├── research.md          # Phase 0 output (completed)
├── data-model.md        # Phase 1 output (completed)
├── quickstart.md        # Phase 1 output (completed)
└── tasks.md             # Phase 2 output (generated by /speckit.tasks)
```

### Source Code (repository root)

```text
backend/
├── src/
│   ├── models/
│   │   └── Transaction.ts                          # NO CHANGES - interface unchanged
│   ├── repositories/
│   │   ├── TransactionRepository.ts                # Modified: ULID + new index + createdAtId in DB items
│   │   ├── TransactionRepository.test.ts           # Modified: Update tests
│   │   └── utils/
│   │       └── Transaction.schema.ts               # NO CHANGES - schema unchanged
│   ├── migrations/
│   │   └── 20251206HHMMSS-populate-createdAtId.ts  # New: Data migration
│   └── scripts/
│       └── create-tables.ts                        # Modified: Update index definition

backend-cdk/
└── lib/
    └── backend-cdk-stack.ts                        # Modified: Update table schema

frontend/
└── [No changes]

frontend-cdk/
└── [No changes]
```

**Structure Decision**: Web application structure (Option 2). This feature modifies backend repository layer and backend-cdk infrastructure only. No frontend changes required since `createdAtId` is an internal repository field not exposed via GraphQL API.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

N/A - No constitution violations. All changes comply with established patterns and principles.

## Phase Completion Status

### ✅ Phase 0: Research (Completed)
- [research.md](./research.md) - Decision rationale, technology choices, alternatives considered

### ✅ Phase 1: Design & Contracts (Completed)
- [data-model.md](./data-model.md) - Entity schema, DynamoDB index design, migration spec
- [quickstart.md](./quickstart.md) - Implementation guide, testing approach, deployment steps
- **contracts/** - N/A (no API changes)

### 🔜 Phase 2: Tasks (Next)
Run `/speckit.tasks` to generate dependency-ordered implementation tasks
