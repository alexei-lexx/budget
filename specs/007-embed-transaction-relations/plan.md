# Implementation Plan: Embed Account and Category Into Transaction GraphQL

**Branch**: `007-embed-transaction-relations` | **Date**: 2025-10-28 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/007-embed-transaction-relations/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

Extend GraphQL API's Transaction type to embed related account and category data directly, eliminating N+1 queries and client-side data joining. Implement batch loading via DataLoader to prevent database query explosion when resolving multiple transactions with their related entities.

**Key Outcomes**:
- GraphQL Transaction type gains `account: TransactionEmbeddedAccount!` and `category: TransactionEmbeddedCategory` fields
- N+1 query reduction: 101 queries (50 transactions) → 3 queries (97% improvement)
- Breaking change: `accountId` and `categoryId` fields removed **immediately** (coordinated single backend+frontend deployment required, short production break acceptable)
- Data consistency edge case handling: missing entities return stub `{ id, name: "Unknown", isArchived: false }` instead of null
- Zero database schema changes; all modifications at GraphQL API layer
- Frontend components simplified: remove client-side lookup functions and maps
- Backend uses DataLoader pattern with per-request scoping for efficient batch loading

## Technical Context

**Language/Version**: TypeScript 4.9+ (Node.js 18+ runtime)
**Primary Dependencies**: Apollo Server 4.x, DataLoader, graphql-js
**Storage**: DynamoDB (existing, no schema changes)
**Testing**: Jest
**Target Platform**: Node.js backend + Vue 3 frontend
**Project Type**: Web (monorepo with 4 npm packages)
**Performance Goals**: N+1 query elimination - batch load 20 transactions into 2-3 DB queries max
**Constraints**: Schema-only breaking change; coordinated backend+frontend deployment; graceful handling of missing entities (stub data)
**Scale/Scope**: Single Transaction type extension affecting 3 user stories with edge case handling

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

✓ **Repository Structure**: Feature affects backend (GraphQL schema + resolvers) and frontend (operations + components), both within established 4-package structure
✓ **Technology Compliance**: Uses Apollo Server (established), adds DataLoader (standard pattern for GraphQL)
✓ **Breaking Change Policy**: Documented in spec with migration strategy (coordinated deployment)
✓ **No Constitution Violations**: Feature follows established backend/frontend patterns

## Project Structure

### Documentation (this feature)

```text
specs/007-embed-transaction-relations/
├── plan.md              # This file (filled by /speckit.plan)
├── spec.md              # Original feature specification
├── research.md          # Phase 0: Research findings (DataLoader, error handling, Vue patterns)
├── data-model.md        # Phase 1: Entity definitions and relationships
├── quickstart.md        # Phase 1: Implementation guide with code templates
├── contracts/           # Phase 1: API contracts
│   └── graphql-schema.graphql  # GraphQL schema changes
└── tasks.md             # Phase 2: Detailed task breakdown (generated by /speckit.tasks)
```

### Source Code (Backend Changes)

```text
backend/
├── src/
│   ├── dataloaders/                   # NEW
│   │   ├── accountLoader.ts           # NEW: Batch load Account entities
│   │   ├── categoryLoader.ts          # NEW: Batch load Category entities
│   │   └── index.ts                   # NEW: Barrel export
│   ├── resolvers/
│   │   ├── Transaction.ts             # MODIFIED: Add account/category field resolvers
│   │   ├── Mutation.ts                # MODIFIED: Add cache invalidation logic
│   │   └── index.ts                   # MODIFIED: Export new Transaction resolvers
│   ├── schema.graphql                 # MODIFIED: New types, extend Transaction
│   └── server.ts                      # MODIFIED: GraphQLContext, initialize DataLoaders
├── package.json                       # MODIFIED: Add dataloader dependency
└── tests/
    └── dataloaders/                   # NEW (optional): Unit tests for batch functions
```

### Source Code (Frontend Changes)

```text
frontend/
├── src/
│   ├── graphql/
│   │   └── fragments.ts               # MODIFIED: Update TRANSACTION_FRAGMENT
│   ├── views/
│   │   └── Transactions.vue           # MODIFIED: Remove lookups, use embedded fields
│   ├── components/
│   │   ├── TransactionCard.vue        # MODIFIED: Direct embedded field access
│   │   └── [other transaction components]  # MODIFIED: Update as needed
│   └── __generated__/                 # AUTO-GENERATED: Updated by npm run codegen
│       ├── graphql-types.ts           # Updated types with embedded fields
│       └── vue-apollo.ts              # Updated composables
└── tests/
    └── components/
        └── TransactionCard.spec.ts    # MODIFIED: Update mocks with embedded structure
```

**Structure Decision**:
Web application with 4 npm packages (backend, frontend, backend-cdk, frontend-cdk).
This feature affects:
1. **Backend**: GraphQL layer only (no database schema changes)
2. **Frontend**: Component layer and GraphQL queries

The feature is isolated to GraphQL API contract changes and view layer updates;
no changes to authentication, routing, or state management architecture.

## Complexity Tracking

> No Constitution violations detected. No complexity justifications needed.

This feature adheres to all established patterns:
- Follows existing 4-package monorepo structure
- Uses established Apollo Server + Vue 3 technologies
- Breaking change is documented and managed via coordinated deployment
- Data isolation maintained through user-scoped repositories

---

## Constitution Check - Phase 1 Re-evaluation

✅ **All Gates Passed**:
- ✓ Repository structure maintained (4 packages, backend/frontend separation)
- ✓ Technology choices align (Apollo 4.x + DataLoader standard pattern)
- ✓ Breaking change documented with migration strategy
- ✓ No architectural violations detected

**Recommended Approval**: Feature design approved for Phase 2 task generation and implementation
