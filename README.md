# Personal Finance Tracker

## Git Hooks

To enable project git hooks, run:

```sh
git config core.hooksPath .githooks
```

To disable project git hooks and use default git hooks, run:

```sh
git config --unset core.hooksPath
```
